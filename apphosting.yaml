# Firebase App Hosting configuration
# This file defines how your application environment is configured.
# Note: Sensitive environment variables like FIREBASE_SERVICE_ACCOUNT 
# are managed directly in the Firebase Console under App Hosting > Settings.
